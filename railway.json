{
  "$schema": "https://railway.app/railway.schema.json",
  "build": {
    "builder": "NIXPACKS",
    "nixpacksPlan": {
      "phases": {
        "install": {
          "commands": ["npm install --omit=dev --ignore-scripts", "chmod +x node_modules/.bin/vite"]
        },
        "build": {
          "commands": ["node ./node_modules/vite/bin/vite.js build"]
        },
        "start": {
          "commands": ["node server.js"]
        }
      },
      "runtime": "nodejs_22"
    }
  },
  "deploy": {
    "numReplicas": 1,
    "restartPolicyType": "ON_FAILURE",
    "restartPolicyMaxRetries": 10
  }
}